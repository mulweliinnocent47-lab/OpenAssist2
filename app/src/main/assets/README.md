Place the uploaded splash video at this exact path before release:

    app/src/main/assets/splash.mp4

The splash screen plays `splash.mp4` for the 5-second launch window, loops it silently while visible, and falls back to the branded card if the asset is missing.
